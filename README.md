# HelloWorld

Simple .NET console app in C#.

Build:

```bash
dotnet build
```

Run:

```bash
dotnet run --project HelloWorld.csproj
```

Or, when in the `HelloWorld` folder:

```bash
dotnet run
```
